# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Redis / Task Queue
redis==5.0.4
arq>=0.26.0

# Elasticsearch
elasticsearch[async]==8.13.1

# MinIO
minio==7.2.5

# LLM / Agents - pinned exact compatible versions (no litellm, use ChatOpenAI directly)
langchain-core==0.2.40
langchain==0.2.16
langchain-openai==0.1.25
langchain-community==0.2.16
langgraph==0.2.0

# PDF Processing
pymupdf==1.24.3
python-magic==0.4.27
docling>=2.0.0
# PDF export (workflow results → markdown → HTML → PDF)
# markdown renders stage outputs to HTML; pygments powers codehilite syntax highlighting.
# weasyprint converts the styled HTML to PDF (chosen over pymupdf.Story due to a
# long-standing API bug in pymupdf.Story.draw that fails with `FzDevice` type errors).
markdown==3.7
pygments==2.19.2
weasyprint==53.4
pydyf==0.10.0

# Document Processing
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2
beautifulsoup4==4.12.3

# Auth
authlib==1.3.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Validation / Serialization
pydantic==2.7.1
pydantic-settings>=2.3.0
pydantic[email]==2.7.1
email-validator==2.1.1

# HTTP Client
httpx==0.27.0

# Utilities
python-dotenv==1.0.1
tenacity==8.3.0
structlog==24.1.0

# Development
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-mock==3.15.1
respx==0.23.1
